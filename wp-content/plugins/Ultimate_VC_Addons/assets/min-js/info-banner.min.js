(function (c) {
    c(document).ready(function () {
        d();
        c(window).resize(function () {
            d()
        });
        jQuery(".ultb3-box .ultb3-info").each(function () {
            if (jQuery(this).attr("data-animation")) {
                jQuery(this).css("opacity", "0");
                var a = jQuery(this).attr("data-animation"), b = "delay-" + jQuery(this).attr("data-animation-delay");
                jQuery(this).bsf_appear(function () {
                    var f = jQuery(this);
                    f.addClass("animated").addClass(a);
                    f.addClass("animated").addClass(b);
                    f.css("opacity", "1")
                }, {accY: -70})
            }
        })
    });
    c(window).load(function () {
        d()
    });
    function d() {
        c(".ultb3-box").each(function (h, j) {
            var b = c(j).outerHeight();
            var i = c(j).find(".ultb3-info").outerHeight();
            var a = (b - i) / 2;
            c(j).find(".ultb3-info").css({top: a})
        })
    }
}(jQuery));